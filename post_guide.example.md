# Post guide (example)

The operator feeds this whole file to the model before every post. Put your voice rules,
banned phrases, pillar definitions and 2–3 complete example posts here — the prompt tells the
model to imitate the restraint of those examples.
